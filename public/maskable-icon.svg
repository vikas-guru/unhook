<svg xmlns="http://www.w3.org/2000/svg" width="512" height="512" viewBox="0 0 512 512">
  <defs>
    <linearGradient id="gm" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0" stop-color="#e0785a"/>
      <stop offset="1" stop-color="#cf6144"/>
    </linearGradient>
  </defs>
  <!-- Full-bleed background: platform applies its own mask over this square. -->
  <rect width="512" height="512" fill="url(#gm)"/>
  <!-- Glyph kept inside the ~80% safe zone. -->
  <text x="50%" y="53%" dominant-baseline="central" text-anchor="middle" font-size="230">🪝</text>
</svg>
